# 依赖
node_modules

# Nuxt 构建产物 / 开发缓存
.nuxt
.output
.data
dist

# Cloudflare 本地状态
.wrangler

# 密钥与环境变量（绝不入库）
.dev.vars
.dev.vars.*
.env
.env.*
!.env.example
# 小程序环境文件不含密钥（仅公开的 API 域名），需要入库
!apps/miniapp/.env.development
!apps/miniapp/.env.production

# 测试/调试产物
screenshots
coverage
*.log

# IDE / 编辑器
.vscode
.idea
.trae

# 操作系统
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN

# TypeScript 增量构建缓存
*.tsbuildinfo

# pnpm workspace
pnpm-lock.yaml.bak

# NestJS 构建产物
apps/api/dist
apps/api/coverage
apps/api/node_modules
# 本地开发时头像等落盘数据
apps/api/data

# Docker 数据（本地开发时挂载到宿主机，不入库）
deploy/data/**
!deploy/data/.gitkeep
deploy/mysql-data
deploy/logs
